# AGENTS.md (Core)

Always start with the compiled bundle, not raw sources.

Preferred:
- `rocs summary --repo .`
- `rocs pack <concept_id> --repo . --profile guiding-circle`

Do not load all concept files into context.
